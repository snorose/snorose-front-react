import {
  Meta,
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  IconGallery,
  IconItem,
  Canvas,
  Story,
} from '@storybook/blocks';

import * as IconStories from './Icon.stories';
import { Icon } from '@/shared/component';
import {
  STATIC_ICON_LIST,
  FILL_ICON_LIST,
  STROKE_ICON_LIST,
  ILLUSTRATION_ICON_LIST,
} from '@/shared/constant';

<Meta of={IconStories} />

<Title>Icon</Title>

<Subtitle>STATIC ICON</Subtitle>
<p>
  <span style={{ color: '#00368E', fontWeight: 'bold' }}>fill</span>과
  <span style={{ color: '#00368E', fontWeight: 'bold' }}>stroke</span>
  prop에 영향을 받지 않는 아이콘입니다.
</p>

<IconGallery>
  {Object.values(STATIC_ICON_LIST).map((icon) => (
    <IconItem name={icon} style={{ backgroundColor: '#333' }}>
        <Icon id={icon} />
    </IconItem>

))}

</IconGallery>
<br/>

<Subtitle>FILL ICON</Subtitle>
<p>
  <span style={{ color: '#00368E', fontWeight: 'bold' }}>fill</span> prop에
  영향을 받는 아이콘입니다.
</p>

<IconGallery>
  {Object.values(FILL_ICON_LIST).map((icon) => (
    <IconItem name={icon} style={{ backgroundColor: '#333' }}>
        <Icon id={icon} />
    </IconItem>

))}

</IconGallery>
<br/>

<Subtitle>STROKE ICON</Subtitle>
<p>
  <span style={{ color: '#00368E', fontWeight: 'bold' }}>stroke</span> prop에
  영향을 받는 아이콘입니다.
</p>

<IconGallery>
  {Object.values(STROKE_ICON_LIST).map((icon) => (
    <IconItem name={icon} style={{ backgroundColor: '#333' }}>
        <Icon id={icon} />
    </IconItem>

))}

</IconGallery>
<br/>

<Subtitle>ILLUSTRATION</Subtitle>
<p>일러스트입니다.</p>

<IconGallery>
  {Object.values(ILLUSTRATION_ICON_LIST).map((icon) => (
    <IconItem name={icon} style={{ backgroundColor: '#333' }}>
        <Icon id={icon} />
    </IconItem>

))}

</IconGallery>
